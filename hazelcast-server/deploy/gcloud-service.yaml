apiVersion: v1
kind: Service
metadata:
  name: hazelcast-server-1
spec:
  selector:
    app: hazelcast-cluster
  ports:
    - protocol: TCP
      port: 8762
      targetPort: 8762
  type: LoadBalancer