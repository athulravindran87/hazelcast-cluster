apiVersion: v1
kind: Service
metadata:
  name: hazelcast-client-1
spec:
  selector:
    app: hazelcast-cluster
  ports:
    - protocol: TCP
      port: 8764
      targetPort: 8764
  type: LoadBalancer